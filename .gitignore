# Runtime artifacts: populated by whichever machine trains the agent
# (normally Colab), pulled down and committed deliberately, never by accident.
# models/*.zip  -- uncommitted for assignment submission (zip policy required in repo)
logs/
plots/
videos/
results.zip

# The experiment log IS committed — it is a deliverable. Ignore only the
# throwaway smoke-test / untrained rows' models above, not this file.
!experiments/experiment_log.csv

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Notebook checkpoints
.ipynb_checkpoints/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
